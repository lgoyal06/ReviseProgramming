package com.lalit.reactive.programming;

public class MatchMeClass {

    public static String matchMe(boolean result1, boolean result2) {
        return result1 && result2 ? "both values are equals" : "values are different";
    }
}
